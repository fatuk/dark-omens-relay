# Сборка (tsc) требует devDependencies: typescript и @types/*.
# При NODE_ENV=production (его выставляет Render) `npm ci` по умолчанию
# пропускает devDependencies — include=dev заставляет ставить их всегда.
include=dev
